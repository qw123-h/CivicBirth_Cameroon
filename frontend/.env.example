# Frontend Environment Variables
# Used by Vite during build and runtime

# API URL (from docker-compose or localhost)
VITE_API_URL=http://localhost:3000

# App environment
VITE_APP_ENV=development
